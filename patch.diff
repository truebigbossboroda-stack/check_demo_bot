diff --git a/docker-compose.yml b/docker-compose.yml
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,7 +1,7 @@
   consumer:
     environment:
       DATABASE_URL: postgresql://postgres:postgres@pg:5432/bot_game_test
       KAFKA_BOOTSTRAP_SERVERS: kafka:19092
-      KAFKA_CONSUMER_GROUP: game-consumer-v1
+      KAFKA_CONSUMER_GROUP: ${KAFKA_CONSUMER_GROUP:-game-consumer-v1}
       KAFKA_DLQ_TOPIC: game-events.dlq
       KAFKA_TOPIC: game-events
diff --git a/docs/demo.md b/docs/demo.md
new file mode 100644
--- /dev/null
+++ b/docs/demo.md
@@ -0,0 +1,40 @@
+# ???: Outbox ? Kafka ? Consumer (PowerShell)
+
+## ?????
+```powershell
+docker compose up -d pg kafka kafka-ui
+docker compose run --rm migrator
+docker compose up -d relay consumer
+docker compose ps
+```
+
+## ??????? ??????? ? outbox (??? ??????? JSON-?????????????)
+```powershell
+$sql = @"
+INSERT INTO outbox_events
+(id,event_type,aggregate_type,aggregate_id,payload,created_at,publish_attempts,last_error,published_at,idempotency_key,status)
+VALUES
+(gen_random_uuid(),'game.finished','game','11111111-1111-1111-1111-111111111111',
+ jsonb_build_object('game_id','11111111-1111-1111-1111-111111111111','chat_id',-5056821738),
+ now(),0,NULL,NULL,concat('demo-', gen_random_uuid()),'new');
+"@
+$sql | docker compose exec -T pg psql -U postgres -d bot_game_test -v ON_ERROR_STOP=1
+```
+
+## ??????? consumed_events
+```powershell
+$sql = @"
+SELECT event_id, event_type, topic, partition, kafka_offset, consumed_at
+FROM consumed_events
+ORDER BY consumed_at DESC NULLS LAST
+LIMIT 10;
+"@
+$sql | docker compose exec -T pg psql -U postgres -d bot_game_test -v ON_ERROR_STOP=1
+```
+
+## Replay (?????????? ?????? ????? ????? consumer-group)
+```powershell
+$env:KAFKA_CONSUMER_GROUP = "game-consumer-replay-" + (Get-Date -Format "yyyyMMdd-HHmmss")
+docker compose up -d --force-recreate consumer
+docker compose logs -n 80 consumer
+```
