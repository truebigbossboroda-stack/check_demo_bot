--- a/consumer.py
+++ b/consumer.py
@@ -53,18 +53,18 @@
     return payload
 
 
-def mark_consumed(db, *, event_id, topic: str, partition: int, offset: int, aggregate_type, aggregate_id, event_type):
+def mark_consumed(db, *, event_id, topic: str, partition: int, kafka_offset: int, aggregate_type, aggregate_id, event_type):
     db.execute(
         sql_text("""
-            INSERT INTO consumed_events (id, event_id, topic, "partition", "offset", aggregate_type, aggregate_id, event_type)
-            VALUES (gen_random_uuid(), CAST(:event_id AS uuid), :topic, :partition, :offset, :aggregate_type, CAST(:aggregate_id AS uuid), :event_type)
+            INSERT INTO consumed_events (id, event_id, topic, "partition", kafka_offset, aggregate_type, aggregate_id, event_type)
+            VALUES (gen_random_uuid(), CAST(:event_id AS uuid), :topic, :partition, :kafka_offset, :aggregate_type, CAST(:aggregate_id AS uuid), :event_type)
             ON CONFLICT (event_id) DO NOTHING
         """),
         {
             "event_id": event_id,
             "topic": topic,
             "partition": partition,
-            "offset": offset,
+            "kafka_offset": kafka_offset,
             "aggregate_type": aggregate_type,
             "aggregate_id": aggregate_id,
             "event_type": event_type,
@@ -224,7 +224,7 @@
                                     event_id=event_id,
                                     topic=record.topic,
                                     partition=record.partition,
-                                    offset=record.offset,
+                                    kafka_offset=record.offset,
                                     aggregate_type=aggregate.get("type"),
                                     aggregate_id=aggregate_id,
                                     event_type=event_type,
@@ -255,7 +255,7 @@
                                     event_id=event_id,
                                     topic=record.topic,
                                     partition=record.partition,
-                                    offset=record.offset,
+                                    kafka_offset=record.offset,
                                     aggregate_type=aggregate.get("type"),
                                     aggregate_id=aggregate_id,
                                     event_type=f"DLQ:{event_type}",
